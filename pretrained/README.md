# Pretrained checkpoints

TMFNet++ changes the original TMFNet architecture, so the original `TMFNet_new.pth`
checkpoint cannot be loaded strictly. Put TMFNet++ checkpoints here or set
`checkpoint` / `pretrained_path` in `configs/tmfnet_pp.json`.
